spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/spring
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


logging:
  level:
    root: warn
server:
  port: 8081
  servlet:
    context-path: /api